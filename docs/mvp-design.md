# DevCareer AI MVP Design Notes

This document mirrors the current build stage: project initialization and foundation architecture.

## Current Stage

- Monorepo root with `web` and `server`
- Vue 3 admin-style shell with placeholder feature pages
- NestJS modular API with placeholder endpoints
- Prisma + SQLite schema prepared for future persistence

## Deferred

- Real AI analysis prompts wired into services
- Full file parsing behavior
- Persisted history workflows
- Polished result rendering and copy actions
