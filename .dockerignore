node_modules
**/node_modules
**/dist
.git
.github
.husky
.vscode
.claude
.agents
docs
server/uploads
*.md
!packages/**/*.md
# 环境变量文件含真实密钥，绝不能进入任何镜像层（.env.example 除外）
**/.env
**/.env.*
!**/.env.example
# web/.env.production 只含构建期公开常量（相对路径 /api），不含密钥，
# 且必须随镜像构建进入 build context，否则生产构建会退回硬编码的 localhost 兜底地址
!web/.env.production
